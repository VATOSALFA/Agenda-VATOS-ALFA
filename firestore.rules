rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // App Check validation has been temporarily disabled for debugging.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
