// This page has been moved to /admin/clients to resolve a routing conflict.
// It is now an empty component to prevent routing issues.
export default function ClientsRedirectPage() {
  return null;
}
