rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // App Check está habilitado. Solo permite acceso a usuarios autenticados
    // desde tu aplicación registrada.
    match /{document=**} {
      allow read, write: if request.auth != null && request.app != null;
    }
  }
}
